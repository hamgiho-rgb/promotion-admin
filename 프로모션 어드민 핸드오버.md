# 프로모션 어드민 — 핸드오버

> **목적**: 다른 PC에서도 똑같이 작업을 이어가기 위한 셋업 + 컨텍스트.
> 클라우드(GitHub + Supabase + Netlify) 기반이라 코드/DB/배포 모두 어디서나 접근 가능.

## ⚡ Claude에게 (다른 PC에서 새 세션 시작할 때)

> 다음 Claude 세션을 시작하면 이 문서를 먼저 읽혀줘. 아래 한 줄을 Cowork에 그대로 붙여넣어 시작하면 돼:
>
> **"`프로모션 어드민 핸드오버.md` 다 읽고 이어서 작업하자"**
>
> 이 문서 + `README.md` + `src/lib/types.ts` + `src/pages/*.tsx` 살펴보면 전체 맥락이 잡혀.

### 사용자 프로필
- 이름: **함기호**, 사업체 **써치(SEARCH)**, 사업자번호 216-21-18212
- 이메일: hamgiho@gmail.com
- 업종: 프로모션/유니폼/브랜드 의류 제조 — 거래처 브랜드들이 주문하면 원단/공임 사서 만들어서 납품
- 대화 스타일: 짧고 직설적 한국어. 가끔 ㅋㅋ, ㅡㅡ 같은 표현. 길게 설명하지 말고 짧고 명확하게.
- 작업 방식: 모바일에서도 봐야 함. 한글 입력 자주 쓰니까 IME 깨짐 조심.

### 기술 스택 (핵심만)
- **React 18 + Vite + TypeScript + Tailwind** (단일 SPA, react-router-dom v6)
- **Supabase** PostgreSQL — URL은 위 환경변수 섹션 참고, 익명 키만 브라우저에서 사용
- **Netlify** GitHub 연동 자동 배포
- **SheetJS (xlsx)** 엑셀 import/export 둘 다

### 디자인 컨벤션
- 모든 페이지 패턴: `PageHeader` + `StatCard` 4개 그리드 + 메인 테이블 + `Drawer` (편집/상세)
- Tailwind: 회색 톤(`zinc-*`), 강조는 검정(`zinc-900`), Badge 색상은 `Badge` 컴포넌트 enum 사용
- 폰트 사이즈: `text-[13px]` 본문, `text-[11px]` 보조, `text-[22px]/[26px]` 페이지 타이틀
- 모바일: Drawer는 풀스크린, 데스크탑은 width prop (sm/md/lg/xl)
- 컬러는 거래처 폴더 안 메모와 함께 `[카테고리]` prefix로 저장 (예: `[원단] | 품목: A, B, C`)

### 중요 컨벤션 — 잊지 말기
1. **한글 IME 버그 방지**: 표 안에서 직접 타이핑하면 매 키스트로크마다 DB 업데이트 → 리렌더 → 한글 조합 깨짐. 반드시 `src/components/ui.tsx`의 **`InlineInput`** 컴포넌트 사용 (composition 이벤트 + onBlur 커밋)
2. **생성 컬럼**: `invoice_items.amount`, `invoice_items.is_return` 은 generated column이라 INSERT에 포함하면 에러. 절대 payload에 넣지 마.
3. **부가세 처리**: 사용자 영수증 양식의 "금일 금액"은 부가세 포함. DB에는 `subtotal` = sum(qty*price), `vat` = subtotal*0.1, `total` = subtotal+vat 로 저장.
4. **placeholder 텍스트 금지**: 사용자가 회색 예시 텍스트(`예: 청운상사`) 싫어함. 모든 입력 필드에서 placeholder 빼.
5. **드로어 닫기 확인**: dirty 상태일 때 닫으면 confirm() 띄움. 새 드로어 만들 때 같은 패턴 적용.
6. **거래처기록들 / 참고자료 / 공장 폴더**: 개인 PC에만 있음 (gitignore). 엑셀 import 테스트할 때만 씀.

### Supabase 스키마 (요약)
```
vendors (id, name, vendor_type[customer/supplier], business_number, ceo_name,
         address, phone, email, bank_info, size_system[] (JSONB),
         memo [형식: "[카테고리] | 품목: A, B, C"])
products (id, code, name, color, vendor_id→vendors, selling_price, notes)
cost_items (id, product_id, supplier_id→vendors, item_name, unit_price, yards,
            subtotal[generated], sort_order)
fabric_usage (id, product_id, color, fabric_in, cut_quantity, total_amount,
              cost_per_unit, yards_per_unit)
incoming (id, vendor_id, issue_date, notes)
incoming_items (id, incoming_id, product_id, product_name, color, size,
                box_number, quantity)
invoices (id, vendor_id, issue_date, supplier_*, bank_info, subtotal, vat, total,
          notes [형식: "[거래처] 시트명"])
invoice_items (id, invoice_id, line_date, product_id, product_name, color,
               quantity, unit_price, amount[generated], is_return[generated],
               sort_order)
quotations / quotation_items (견적서)
```

### 현재 데이터 현황 (2026-05-11 기준)
- 고객 거래처 **30개** (가이던스, 일리오, 마요네즈, 청운상사, 김메주, 노잉, 비벨로체 등)
- 공급처 **79개** (원단 60 + 공임 7 + 립 3 + 자수 3 + 부자재 2 + 나염 2 + 워싱 1 + 기타 1)
- 계산서 **238건**, 합계 ₩9.76억
- 거래처별 사이즈 체계: 청운상사=110~170 (아동), 마요네즈=1,2, 일리오=S/M/L, 기타 거래처별로 다름
- 거래처/공급처 memo에 "품목: A,B,C" 형식으로 취급품목 가등록되어 있음 (편집은 거래처 수정 화면의 chip-입력 UI)

### 완료된 큰 작업들 (시간순)
1. Supabase 스키마 + RLS 익명 허용 (`supabase-*.sql`)
2. 사이드바 + Layout (모바일 햄버거 메뉴)
3. 거래처 (고객/공급처 분리) + 카테고리 (원단/립/나염/자수/부자재/워싱/라벨/공임/포장/기타)
4. 상품/원가계산서/원단(실 입고)/입고내역서/계산서/견적서/마진내역서 페이지
5. 계산서 인쇄(`InvoicePrint.tsx`) + 견적서 인쇄(`QuotationPrint.tsx`)
6. 엑셀 가져오기 (`DataImport.tsx`) — 헤더 자동 인식
7. 거래처기록들 폴더 → 26개 고객, 238 계산서, 1869 라인 일괄 등록
8. 일리오계산서.xlsx 33개 시트 등록 (B=날짜 D=품명 F=품목 G=사이즈 H=수량 I=단가 J=금액)
9. 원단 공급처 64곳 가등록 (강인텍스, 신상텍스, 두원, 로얄텍스 등)
10. 거래처/공급처 메모에 취급품목 카탈로그 가등록 (62공급처×219품목, 26고객×313품목)
11. 카탈로그 UI: 칩 → 깔끔한 2~3열 그리드
12. 엑셀 내보내기 (`src/lib/exportXlsx.ts`) — 계산서는 사용자 영수증 양식 그대로 (B2 영 수 증, 공급자 박스, 날짜/품명/품목/사이즈/수량/단가/금액 표, J32=SUM/I33=*0.1/I34=I32+I33)
13. 배포 준비 (Netlify env 분리, .gitignore 강화) + 핸드오버 문서

### 남은 작업 / 사용자가 가능성 있게 요청할 것들
- 실제 첫 GitHub push + Netlify 연동 (사용자가 first-push.bat 실행)
- 공장 폴더 매입 계산서 등록 (사용자가 "공장이라는 폴더는 그냥 둬 따로 작업해" 라고 했음 — 별도 작업)
- 카테고리 잘못 분류된 공급처 추가 정리 (사용자가 발견하는 대로)
- 모바일 사용성 추가 다듬기
- 매출/마진 차트 고도화

### 코드 베이스 빠른 안내
- `src/lib/supabase.ts` — Supabase 클라이언트 (env로 키 주입)
- `src/lib/types.ts` — TS 인터페이스 모음
- `src/lib/exportXlsx.ts` — **영수증 양식 엑셀 빌더** 여기에 있음
- `src/components/ui.tsx` — Button, Input, **InlineInput**(한글IME), Select, Textarea, Drawer, Empty, Badge, Label, PageHeader
- `src/components/Sidebar.tsx` — 메뉴 구조 (메인/거래처/상품·원가/영업·운영/분석/도구)
- `src/components/Layout.tsx` — 모바일 햄버거 + 슬라이드인 사이드바
- `src/components/SizePicker.tsx` — 사이즈 옵션 칩 선택
- `src/pages/*.tsx` — 각 페이지 (Customers/Suppliers/Products/ProductDetail/CostBreakdown/Fabric/Incoming/Invoices/InvoicePrint/Quotations/QuotationPrint/MarginReport/DataImport/Dashboard)

### 사용자가 자주 쓰는 표현
- "엎어치기" = 처음부터 다시 만들기 (괜찮음, 두려워하지 마)
- "ㅡㅡ" = 좀 짜증남 (가벼움)
- "ㅋㅋ" = 가벼운 농담
- 짧고 명확하게 물어보고 빨리 결과 보여주는 걸 선호

---

---

## 1. 한눈에 보기

| 항목 | 값 |
|---|---|
| 사업체 | 써치(SEARCH) · 함기호 · 216-21-18212 |
| 코드 저장소 | GitHub (배포 후 URL 입력) |
| DB | Supabase — `https://gnjjninntiwbbzmfcnqt.supabase.co` |
| 배포 | Netlify (자동 배포) |
| 라이브 URL | (Netlify 배포 후 입력) |
| 기술 스택 | React 18 + Vite + TypeScript + Tailwind + Supabase |

---

## 2. 다른 PC에서 처음 셋업하는 순서

### 2-1. Node.js 설치
1. https://nodejs.org → **LTS 버전** 다운로드 (현재 20.x)
2. 설치 후 새 터미널 열고 `node -v` 확인

### 2-2. Git 설치
1. https://git-scm.com/download/win → 설치
2. 새 터미널에서 `git --version` 확인
3. 처음이라면 사용자 정보 등록:
   ```bash
   git config --global user.name "함기호"
   git config --global user.email "hamgiho@gmail.com"
   ```

### 2-3. 코드 가져오기 (GitHub 연동 후)
```bash
cd C:\Users\User\Desktop\COWORK
git clone https://github.com/<본인계정>/promotion-admin.git 프로모션어드민
cd 프로모션어드민
```

### 2-4. `.env` 파일 만들기
프로젝트 루트에 `.env` 파일 생성 (보안상 git엔 안 올려져 있음):
```
VITE_SUPABASE_URL=https://gnjjninntiwbbzmfcnqt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_1WFts_MStSQGujnaZfswiw_K6Ds1SKI
```
> 💡 `.env.example` 참고. **퍼블리셔블(Anon) 키만** 적어. 시크릿 키는 절대 .env 안 들어감.

### 2-5. 패키지 설치 & 실행
```bash
npm install
npm run dev
```
`http://localhost:5173` 열림. 로그인 없이 바로 사용 가능 (Supabase RLS는 익명 허용 상태).

---

## 3. 배포 (Netlify + GitHub)

### 3-1. GitHub 저장소 만들기 (한 번만)
1. https://github.com/new
2. 이름: `promotion-admin` (private 권장)
3. README/license 추가 X (이미 있음)
4. 생성 후 URL 복사

### 3-2. 로컬에서 첫 푸시 (한 번만)
```bash
cd C:\Users\User\Desktop\COWORK\프로모션어드민
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/<본인계정>/promotion-admin.git
git push -u origin main
```

### 3-3. Netlify 연동
1. https://app.netlify.com → **Add new site → Import an existing project**
2. **GitHub** 선택 → `promotion-admin` 저장소 선택
3. Build settings:
   - Build command: `npm run build` (이미 netlify.toml에 있음)
   - Publish directory: `dist`
4. **Environment variables** 추가 (필수):
   - `VITE_SUPABASE_URL` = `https://gnjjninntiwbbzmfcnqt.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = `sb_publishable_1WFts_MStSQGujnaZfswiw_K6Ds1SKI`
5. **Deploy** 누르면 1~2분 후 라이브.
6. 이후 GitHub `main` 브랜치에 push 할 때마다 자동 재배포.

### 3-4. 커스텀 도메인 (선택)
Netlify → Domain management 에서 본인 도메인 연결.

---

## 4. 매일의 작업 흐름

```bash
# 작업 시작 — 최신 코드 받아오기
cd C:\Users\User\Desktop\COWORK\프로모션어드민
git pull

# 개발 서버 띄우기
npm run dev

# 코드 수정 → 저장 → 브라우저 자동 새로고침

# 작업 끝나면 커밋 & 푸시 (자동 배포됨)
git add .
git commit -m "수정 내용 한 줄 설명"
git push
```

같은 와이파이의 휴대폰에서 접속하려면 `npm run dev` 실행 후 출력되는 **Network: `http://192.168.x.x:5173`** 주소를 폰 브라우저에 입력.

---

## 5. Supabase 정보

| 항목 | 값 |
|---|---|
| 프로젝트 URL | https://supabase.com/dashboard/project/gnjjninntiwbbzmfcnqt |
| 익명 키 (브라우저용, 공개 가능) | `sb_publishable_1WFts_MStSQGujnaZfswiw_K6Ds1SKI` |
| 시크릿 키 (서버용, 절대 노출 X) | `API키메모.txt` 파일에 보관 — git에 안 올라감 |

### 테이블 구조 (이미 만들어져 있음)
- `vendors` — 거래처 (고객/공급처 + memo에 `[카테고리] \| 품목: ...` 형식)
- `products` — 상품 (스타일)
- `cost_items` — 원가계산서 라인
- `fabric_usage` — 원단 입고/재단
- `incoming` / `incoming_items` — 입고내역서
- `invoices` / `invoice_items` — 계산서 (subtotal/vat/total은 사용자 입력, amount는 생성 컬럼)
- `quotations` / `quotation_items` — 견적서

새 Supabase 프로젝트에 그대로 옮기고 싶으면:
1. `supabase-schema.sql` 실행 (테이블 생성)
2. `supabase-add-quotations.sql` 실행 (견적서 테이블)
3. `supabase-allow-anon.sql` 실행 (익명 액세스 허용)

---

## 6. 현재 데이터 현황 (2026-05-11 기준)

- 고객 거래처 **30개** (가이던스, 일리오, 마요네즈, 청운상사 등)
- 공급처 **79개** (원단 60, 공임 7, 립 3, 자수 3, 부자재 2, 나염 2, 워싱 1, 기타 1)
- 계산서 **238건**, 합계 ₩9.76억
- 입고내역 / 상품 / 원가 / 견적 등은 사용 시작하면 누적

> 💡 데이터는 모두 Supabase 서버에 있어서 어떤 PC에서 접속해도 동일하게 보임.

---

## 7. 핵심 파일 위치

```
프로모션어드민/
├── src/
│   ├── lib/
│   │   ├── supabase.ts       # Supabase 클라이언트
│   │   ├── types.ts          # 타입 정의
│   │   └── exportXlsx.ts     # 엑셀 내보내기 유틸 (영수증 양식 포함)
│   ├── components/
│   │   ├── Sidebar.tsx       # 좌측 메뉴
│   │   ├── Layout.tsx        # 전체 레이아웃 + 모바일 햄버거
│   │   ├── SizePicker.tsx    # 사이즈 옵션 선택
│   │   └── ui.tsx            # 공용 UI (Button, Input, Drawer, InlineInput …)
│   └── pages/
│       ├── Dashboard.tsx     # 대시보드
│       ├── Customers.tsx     # 고객 거래처
│       ├── Suppliers.tsx     # 공급처
│       ├── Products.tsx      # 상품 관리
│       ├── ProductDetail.tsx # 리오더 보기
│       ├── CostBreakdown.tsx # 원가계산서
│       ├── Fabric.tsx        # 실 입고 내역
│       ├── Incoming.tsx      # 입고내역서
│       ├── Invoices.tsx      # 계산서
│       ├── InvoicePrint.tsx  # 계산서 인쇄
│       ├── Quotations.tsx    # 견적서
│       ├── QuotationPrint.tsx
│       ├── MarginReport.tsx  # 마진내역서
│       └── DataImport.tsx    # 엑셀 가져오기
├── supabase-*.sql            # DB 스키마
├── netlify.toml              # Netlify 빌드 설정
├── package.json              # 의존성
├── README.md / HOW_TO_RUN.md / HANDOVER.md
└── .env                      # 환경 변수 (git 제외)
```

---

## 8. 자주 쓰는 작업

### 거래처 폴더 엑셀 일괄 등록
1. 엑셀 파일들을 `거래처기록들/<폴더명>/` 안에 넣음 (개인 PC에서만, git 안 올라감)
2. 앱 좌측 메뉴 → **엑셀 가져오기** → 파일 선택 → 자동 인식

### 새 계산서 발행
1. **계산서/영수증** → **＋ 새 계산서**
2. 거래처 선택 → 라인 입력 (또는 입고내역서에서 가져오기)
3. 저장 → 출력(🖨️) 또는 엑셀 다운로드(📥) 가능

### 엑셀 내보내기 (영수증 양식 그대로)
각 페이지 상단 **📥 엑셀 내보내기** 버튼. 계산서는 일리오/청운상사 양식과 동일 (B2 영수증, 공급자 박스, 표, 합계 수식).

---

## 9. 문제 해결

| 증상 | 해결 |
|---|---|
| `npm install` 실패 | Node.js 버전 확인 (`node -v` ≥ 20). 안 되면 재설치. |
| 로그인 안 됨 | 현재 로그인 비활성. Supabase 대시보드 → Auth → "Confirm email" OFF |
| `.env` 만든 후 안 읽힘 | dev 서버 재시작 (Ctrl+C → `npm run dev`) |
| Netlify 빌드 실패 | Netlify Environment variables에 `VITE_SUPABASE_*` 들어갔는지 확인 |
| 한글 입력 시 글자 깨짐 | 표 안 인라인 입력은 `InlineInput` 사용 (이미 적용됨). 만약 새 입력 추가하면 `<InlineInput>` 사용 |
| TypeScript 에러로 빌드 실패 | `package.json`에서 build 스크립트는 `vite build` 만 — tsc 에러 무시. 타입 체크하려면 `npm run typecheck` |

---

## 10. 연락 / 다음 단계

- 새 기능 추가는 Claude한테 부탁하면 됨
- 큰 변경 전엔 GitHub에 백업 푸시 먼저
- Supabase 백업: Dashboard → Database → Backups 에서 매일 자동 백업됨 (7일 보관)
