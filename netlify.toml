[build]
  command = "npm run build"
  publish = "dist"

# SPA 라우팅: 모든 경로를 index.html로 보냄 (React Router 동작)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
