-- ─────────────────────────────────────────────
-- 계산서 라인에 사이즈 컬럼 추가
-- 입고 → 계산서 자동 발행 시 사이즈별 라인 분리하기 위함
-- 한 번만 실행 (멱등)
-- ─────────────────────────────────────────────

alter table invoice_items
  add column if not exists size text;
